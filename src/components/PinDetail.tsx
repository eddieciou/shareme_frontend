import React from 'react';
import { TUser } from '../types';

interface IPinDetail {
  user: TUser;
}

const PinDetail = ({ user }: IPinDetail) => {
  return <div>PinDetail</div>;
};

export default PinDetail;
