import React from 'react';
import { TUser } from '../types';

interface ICreatePin {
  user: TUser;
}

const CreatePin = ({ user }: ICreatePin) => {
  return <div>CreatePin</div>;
};

export default CreatePin;
